{
 "id": "enum-gate",
 "kind": "enum",
 "family": "gate",
 "path": "templates-enum/gate.json",
 "note": "The fixed vocabulary of templates-gate/, mined from 6 of its 6 templates by enums.mjs. Kept in templates-enum/, never inside templates-gate/ itself, so nothing that scans templates-gate/ for its own templates ever trips over this file. A field with more than 100 distinct values, or one that is close to a different value in every file, is not here — it is data, not an enum. Edit the source templates and re-run enums.mjs to update this.",
 "totalFiles": 6,
 "sampledFiles": 6,
 "fields": {
  "lintel_d": {
   "type": "number",
   "distinct": 4,
   "samples": 6,
   "values": [
    0,
    1,
    1.1,
    1.2
   ]
  },
  "material": {
   "type": "string",
   "distinct": 4,
   "samples": 6,
   "values": [
    "limestone",
    "sandstone",
    "timber on masonry",
    "wrought iron"
   ]
  },
  "device": {
   "type": "string",
   "distinct": 4,
   "samples": 6,
   "values": [
    "none — nothing is above it",
    "relieving triangle",
    "timber lintel",
    "true arch"
   ]
  },
  "leaves": {
   "type": "number",
   "distinct": 1,
   "samples": 6,
   "values": [
    2
   ]
  },
  "strength": {
   "type": "number",
   "distinct": 4,
   "samples": 6,
   "values": [
    0.15,
    0.75,
    0.9,
    1
   ]
  }
 },
 "wovenBy": "enums.mjs"
}