{
 "id": "enum-heze",
 "kind": "enum",
 "family": "heze",
 "path": "templates-enum/heze.json",
 "note": "The fixed vocabulary of templates-heze/, mined from 10 of its 10 templates by enums.mjs. Kept in templates-enum/, never inside templates-heze/ itself, so nothing that scans templates-heze/ for its own templates ever trips over this file. A field with more than 100 distinct values, or one that is close to a different value in every file, is not here — it is data, not an enum. Edit the source templates and re-run enums.mjs to update this.",
 "totalFiles": 10,
 "sampledFiles": 10,
 "fields": {
  "kind": {
   "type": "string",
   "distinct": 1,
   "samples": 10,
   "values": [
    "heze-slide"
   ]
  },
  "rule": {
   "type": "boolean",
   "distinct": 1,
   "samples": 10,
   "values": [
    true
   ]
  },
  "note.strong": {
   "type": "string",
   "distinct": 6,
   "samples": 10,
   "values": [
    "Build the ten.",
    "The check that actually matters.",
    "The real-world version of this mistake.",
    "What this is not.",
    "Why it has to be this way.",
    "Why the floor matters."
   ]
  },
  "wovenBy": {
   "type": "string",
   "distinct": 1,
   "samples": 10,
   "values": [
    "the bridge"
   ]
  },
  "okFlag.strong": {
   "type": "string",
   "distinct": 1,
   "samples": 10,
   "values": [
    "Nothing leaves the file."
   ]
  },
  "gridCols": {
   "type": "number",
   "distinct": 1,
   "samples": 10,
   "values": [
    2
   ]
  },
  "hero": {
   "type": "string",
   "distinct": 1,
   "samples": 10,
   "values": [
    "HEZE"
   ]
  },
  "band": {
   "type": "boolean",
   "distinct": 1,
   "samples": 10,
   "values": [
    true
   ]
  },
  "ledeAfter": {
   "type": "boolean",
   "distinct": 1,
   "samples": 10,
   "values": [
    true
   ]
  }
 },
 "wovenBy": "enums.mjs"
}