{
 "id": "enum-instrument",
 "kind": "enum",
 "family": "instrument",
 "path": "templates-instrument/_enum.json",
 "note": "The fixed vocabulary of templates-instrument/, mined from 12 of its 12 templates by enums.mjs. A field with more than 100 distinct values, or one that is close to a different value in every file, is not here — it is data, not an enum. Edit the source templates and re-run enums.mjs to update this.",
 "totalFiles": 12,
 "sampledFiles": 12,
 "fields": {
  "uncertainty": {
   "type": "number",
   "distinct": 9,
   "samples": 12,
   "values": [
    0.01,
    0.02,
    0.1,
    0.5,
    1,
    8,
    25,
    50,
    400
   ]
  },
  "kind": {
   "type": "string",
   "distinct": 3,
   "samples": 12,
   "values": [
    "computed",
    "derived",
    "measured"
   ]
  },
  "rounded_from": {
   "type": "number",
   "distinct": 2,
   "samples": 12,
   "values": [
    574,
    2363
   ]
  }
 },
 "wovenBy": "enums.mjs"
}